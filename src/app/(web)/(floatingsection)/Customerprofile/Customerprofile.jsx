'use client'

import styles from './Customerprofile.module.css'

const images = [
 
];

export default function Customerprofile({ }) {

    return (
        <div className="">
   
        </div>
    );
}