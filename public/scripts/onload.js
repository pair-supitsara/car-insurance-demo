(function () { /* ใช้ IIFE กันชน namespace */
    /*fetch('/api/carinsurance/fnGetuserinfo', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ userId: 123 })
    })
    .then(res => res.json())
    .then(res => {
        window.__STORE__?.dispatch({
          type: 'customer/setinfo',
          payload: {
            name: res.data[0]?.name || null
          }
        })
    }).then(() => 
      console.log(window.__STORE__)
    )*/
})();